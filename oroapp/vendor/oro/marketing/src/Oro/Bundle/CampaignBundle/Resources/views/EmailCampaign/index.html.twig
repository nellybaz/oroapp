{% extends 'OroUIBundle:actions:index.html.twig' %}
{% import 'OroUIBundle::macros.html.twig' as UI %}

{% set pageTitle = 'oro.campaign.emailcampaign.entity_plural_label'|trans %}
{% set gridName = 'oro-email-campaign-grid' %}

{% block navButtons %}
    <div class="btn-group">
        {% if is_granted('oro_email_campaign_create') %}
            {{ UI.addButton({
                'path': path('oro_email_campaign_create'),
                'entity_label': 'oro.campaign.emailcampaign.entity_label'|trans
            }) }}
        {% endif %}
    </div>
{% endblock %}
